{
  "name": "console-log-interceptor",
  "version": "1.1.6",
  "description": "console.log interceptor for grab your console.logs",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "npx tsc",
    "test": "mocha --exit --recursive 'test/**/*.test.ts'",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "console.log",
    "interceptor",
    "log-reader",
    "log-interceptor",
    "console-log-interceptor"
  ],
  "author": "swimmiee",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "@types/mocha": "^9.1.1",
    "chalk": "^4.1.2",
    "mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "fs-extra": "^10.1.0"
  }
}
